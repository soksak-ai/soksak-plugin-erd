import type { NodeTypes } from '@xyflow/react';
import { TableNode } from './TableNode';
import { LayerNode } from './LayerNode';

export const nodeTypes: NodeTypes = { table: TableNode, layer: LayerNode };
